{
  // 1. Declare a typed array that only recieves numbers.
  //    Assign it an array of 3 numbers.
  const myNumArr: number[] = [30, 58, 14]

  // 2. Declare a variable that expects only numbers.
  //    Assign it a number value
  const myNum: number = 42

  // 3. "Push" the variable into the array.
  myNumArr.push(myNum)

  // 4. Log the array out in the console.
  console.log(myNumArr)

}
