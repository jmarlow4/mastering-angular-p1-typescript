{
  // 1. Declare and define a typed array that only recieves numbers.

  // 2. Declare and define a variable that expects only numbers.

  // 3. "Push" the variable into the array.

  // 4. Log the array out in the console.

}
